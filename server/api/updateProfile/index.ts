export type Methods = {
  post: {
    reqBody: {
      userId: string;
      like: string;
      newName: string;
      game: string;
      zisseki: string;
      selectedFile: string;
    };
  };
};
